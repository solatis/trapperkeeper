{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://trapperkeeper.dev/schemas/frontmatter-hub.schema.json",
  "title": "Hub Document Frontmatter",
  "description": "Metadata schema for hub documents in Trapperkeeper documentation",
  "type": "object",
  "required": [
    "doc_type",
    "status",
    "date_created",
    "primary_category"
  ],
  "properties": {
    "doc_type": {
      "type": "string",
      "enum": ["spoke", "hub", "index", "guide", "reference", "redirect-stub"],
      "description": "Type of documentation. Must be 'hub' for hub documents."
    },
    "status": {
      "type": "string",
      "enum": ["draft", "active", "deprecated", "superseded"],
      "description": "Current status of the document in its lifecycle"
    },
    "date_created": {
      "type": "string",
      "format": "date",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Date document was created (YYYY-MM-DD)"
    },
    "date_updated": {
      "type": "string",
      "format": "date",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Date of last significant update (YYYY-MM-DD)"
    },
    "primary_category": {
      "type": "string",
      "enum": [
        "architecture",
        "api",
        "database",
        "security",
        "performance",
        "validation",
        "configuration",
        "testing",
        "deployment",
        "error-handling"
      ],
      "description": "Primary domain category for this document"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true,
      "description": "Additional classification tags for discovery and indexing"
    },
    "consolidated_spokes": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true,
      "minItems": 3,
      "description": "List of spoke document paths or identifiers consolidated by this hub. Minimum 3 spokes required."
    },
    "cross_cutting": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["security", "performance", "validation", "observability", "error-handling"]
      },
      "uniqueItems": true,
      "description": "Cross-cutting concerns this hub addresses. Used for index generation."
    },
    "depends_on": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true,
      "description": "Foundational documents that must be understood before this hub. Use document identifiers or relative paths."
    },
    "extended_by": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true,
      "description": "Documents that extend or build upon this hub. Use document identifiers or relative paths."
    },
    "authors": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Primary authors or maintainers of this document"
    },
    "maintainer": {
      "type": "string",
      "description": "Primary maintainer responsible for hub content accuracy and spoke synchronization"
    }
  },
  "not": {
    "anyOf": [
      {
        "required": ["version"]
      },
      {
        "required": ["revision"]
      },
      {
        "required": ["changelog"]
      },
      {
        "required": ["history"]
      },
      {
        "required": ["versions"]
      },
      {
        "required": ["revisions"]
      }
    ]
  }
}
