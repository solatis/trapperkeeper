# cmd/trapperkeeper/

Entry point for TrapperKeeper CLI binary.

## Index

| File       | Contents (WHAT)                            | Read When (WHEN)                  |
| ---------- | ------------------------------------------ | --------------------------------- |
| `main.go`  | CLI entry point, delegates to cmd package  | Modifying binary build process    |
| `cmd/`     | Cobra commands (root, sensor-api, etc.)    | Adding new commands, debugging    |
